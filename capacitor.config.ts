import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mangastoryforge.app',
  appName: 'Manga Story Forge',
  webDir: 'dist'
};

export default config;
